name: deploy prod
on:
  push:
    branches: [main]

jobs:
  one:
    name: deploy prod action
    runs-on: ubuntu-latest
    steps:
      - name: SSH Remote Commands
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ***REMOVED***
          username: clove
          key: ${{ secrets.MAINMAC }}
          port: 20221
          script: |
            cd /home/clove/notclove.ru/content
            sudo git pull https://github.com/NotClove/nc-content.git
            cd /home/clove/notclove.ru
            sudo hugo -D


